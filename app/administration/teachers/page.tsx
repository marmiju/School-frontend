import Administration from '@/lib/administration/Administration';
import React from 'react';

const page = () => {
    const teachers = Administration({cetagory: 'teachers'});
    return (
        <div>
            
        </div>
    );
};

export default page;